import { expect } from "chai";
import {
  ConstraintsUtil,
  ValidationUtil,
  ValidatorUtil,
} from "./index";

describe("index.ts", () => {
  describe("#ConstraintsUtil", () => {
    it("expect ConstraintsUtil not to equal undefined ", () => {
      // arranges

      // acts

      // asserts
      expect(ConstraintsUtil).not.to.equal(undefined);
    });
  });

  describe("#ValidationUtil", () => {
    it("expect ValidationUtil not to equal undefined ", () => {
      // arranges

      // acts

      // asserts
      expect(ValidationUtil).not.to.equal(undefined);
    });
  });

  describe("#ValidatorUtil", () => {
    it("expect ValidatorUtil not to equal undefined ", () => {
      // arranges

      // acts

      // asserts
      expect(ValidatorUtil).not.to.equal(undefined);
    });
  });
});
