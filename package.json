{
  "name": "supervalidation-repos",
  "private": true,
  "scripts": {
    "init": "lerna init",
    "setup": "lerna bootstrap",
    "build": "lerna run build",
    "release": "lerna publish --force-publish --exact"
  },
  "devDependencies": {
    "lerna": "3.22.1"
  }
}
